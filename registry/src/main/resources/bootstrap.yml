spring:
  application:
    name: registry
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://config:8888}
      fail-fast: true
      username: user
      password: ${CONFIG_SERVICE_PASSWORD:dev}